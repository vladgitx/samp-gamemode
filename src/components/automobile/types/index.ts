import { WorldPosition } from "open-godfather"

export type AutomobileData = {
    id: number
    model: number
    position: WorldPosition
    rotation: number
    primaryColor: number
    secondaryColor: number
    plate: string
}